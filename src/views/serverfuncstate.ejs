<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="../css/cssdir/out.css" rel="stylesheet" />
  <title>Gerenciador de Funções</title>
</head>

<body class=" min-h-screen flex items-center justify-center">
  <div class="pages_cont p-8 rounded-lg shadow-md w-full max-w-7xl">
    <div class="flex items-center gap-5 px-4 sm:px-0">
      <div class="flex-shrink-0">
        <% if (info.icon && info.icon.indexOf('null') === -1) { %>
        <img src="<%= info.icon %>" alt="Ícone da guilda" class="w-24 h-24 rounded-lg object-cover" />
        <% } else { %>
        <img src="/res/serverIcon/nonserverimg.jpg" alt="Ícone padrão" class="w-24 h-24 rounded-lg object-cover" />
        <% } %>
      </div>
      <div class="flex-1">
        <h3 class="text-base/7 font-semibold text-white">Funções do Servidor</h3>
        <p class="mt-1 max-w-2xl text-sm/6 text-gray-400">Visão geral das funções ativas e configurações para a guilda <%= info.name %>.</p>
      </div>
    </div>

    <div class="flex max-w-7xl mt-8">
      <table class="w-full table-fixed divide-y divide-white-700">
        <thead>
          <tr>
            <th scope="col" class="w-[15%] py-3.5 pl-4 pr-3 text-left text-sm font-semibold text-white sm:pl-0">ID Guilda</th>
            <th scope="col" class="w-[15%] px-3 py-3.5 text-left text-sm font-semibold text-white">Nome</th>
            <th scope="col" class="w-[15%] px-3 py-3.5 text-left text-sm font-semibold text-white">YouTube Notify</th>
            <th scope="col" class="w-[15%] px-3 py-3.5 text-left text-sm font-semibold text-white">Twitch Notify</th>
            <th scope="col" class="w-[20%] px-3 py-3.5 text-left text-sm font-semibold text-white">Canal YouTube</th>
            <th scope="col" class="w-[20%] px-3 py-3.5 text-left text-sm font-semibold text-white">Canal Twitch</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-800">
          <tr>
            <td class="truncate py-4 pl-4 pr-3 text-sm font-medium text-white sm:pl-0"><%= info4.guildID %></td>
            <td class="truncate px-3 py-4 text-sm text-gray-300"><%= info.name %></td>
            <td class="truncate px-3 py-4 text-sm text-gray-300"><%= info4.youtubenotify %></td>
            <td class="truncate px-3 py-4 text-sm text-gray-300"><%= info4.twitchnotify %></td>
            <td class="truncate px-3 py-4 text-sm text-gray-300"><%= info4.channelytb %></td>
            <td class="truncate px-3 py-4 text-sm text-gray-300"><%= info4.channeltch %></td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</body>
</html>