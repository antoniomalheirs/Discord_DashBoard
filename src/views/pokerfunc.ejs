<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/css/cssdir/out.css" rel="stylesheet" />
    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <title>Poker Experience | Config</title>
    <style>
        .casino-gradient {
            background: linear-gradient(135deg, #1a1c20 0%, #0f0f10 100%);
        }

        .card-glow {
            position: absolute;
            width: 300px;
            height: 300px;
            background: radial-gradient(circle, rgba(220, 38, 38, 0.15) 0%, rgba(0, 0, 0, 0) 70%);
            border-radius: 50%;
            pointer-events: none;
            z-index: 0;
        }

        .input-dark {
            background-color: rgba(17, 24, 39, 0.8);
            border: 1px solid rgba(55, 65, 81, 0.5);
            transition: all 0.3s ease;
        }

        .input-dark:focus {
            background-color: rgba(17, 24, 39, 1);
            border-color: #ef4444;
            /* Red-500 */
            box-shadow: 0 0 0 2px rgba(239, 68, 68, 0.2);
        }
    </style>
</head>

<body class="bg-[#0b0c10] font-sans antialiased text-gray-100 min-h-screen flex flex-col relative overflow-x-hidden">

    <!-- Background Elements -->
    <div class="fixed top-0 left-0 w-full h-full overflow-hidden pointer-events-none z-0">
        <div class="absolute top-[-10%] left-[-10%] w-[40%] h-[40%] bg-red-600/10 rounded-full blur-[120px]"></div>
        <div class="absolute bottom-[-10%] right-[-10%] w-[40%] h-[40%] bg-orange-600/10 rounded-full blur-[120px]">
        </div>
    </div>

    <!-- Navbar -->
    <nav
        class="bg-gray-900/60 backdrop-blur-xl border-b border-gray-800/60 fixed w-full z-50 transition-all duration-300">
        <div class="mx-auto max-w-7xl px-6">
            <div class="flex h-20 items-center justify-between">
                <div class="flex items-center gap-4">
                    <div
                        class="h-12 w-12 rounded-2xl bg-gradient-to-br from-red-600 to-red-800 flex items-center justify-center shadow-lg shadow-red-900/30">
                        <i class="fa-solid fa-heart text-white text-xl"></i>
                    </div>
                    <div>
                        <span class="block text-xl font-bold text-white tracking-wide">Poker Manager</span>
                        <span class="block text-xs text-gray-400 font-medium tracking-wider uppercase">Módulo
                            Premium</span>
                    </div>
                </div>
                <button onclick="window.history.back()"
                    class="group flex items-center gap-2 text-gray-400 hover:text-white transition-all bg-gray-800/50 hover:bg-gray-800 px-4 py-2 rounded-xl border border-gray-700/50 hover:border-gray-600">
                    <i class="fa-solid fa-arrow-left text-sm group-hover:-translate-x-1 transition-transform"></i>
                    <span class="text-sm font-medium">Voltar</span>
                </button>
            </div>
        </div>
    </nav>

    <main class="flex-grow pt-32 pb-20 px-6 relative z-10 flex flex-col items-center justify-center">

        <!-- Header -->
        <div class="text-center mb-16 animate-fade-in-up">
            <div
                class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-red-900/20 border border-red-500/20 text-red-400 text-xs font-bold uppercase tracking-widest mb-4">
                <span class="w-1.5 h-1.5 rounded-full bg-red-500 animate-pulse"></span>
                Configuração de Jogo
            </div>
            <h1
                class="text-5xl md:text-6xl font-black text-transparent bg-clip-text bg-gradient-to-r from-gray-100 via-gray-200 to-gray-400 mb-6 drop-shadow-sm">
                Mesa de <span
                    class="text-transparent bg-clip-text bg-gradient-to-r from-red-500 to-orange-500">Poker</span>
            </h1>
            <p class="text-lg text-gray-400 max-w-2xl mx-auto leading-relaxed">
                Centralize a ação. Defina um canal exclusivo para as mesas de Texas Hold'em e mantenha seu servidor
                organizado enquanto a diversão acontece.
            </p>
        </div>

        <!-- Main Card -->
        <div class="w-full max-w-4xl relative group perspective-1000">
            <!-- Glow Behind -->
            <div
                class="absolute -inset-1 bg-gradient-to-r from-red-600/30 to-orange-600/30 rounded-3xl blur-2xl opacity-50 group-hover:opacity-75 transition duration-1000">
            </div>

            <div
                class="relative bg-[#121418] border border-gray-700/50 rounded-3xl p-8 md:p-12 shadow-2xl backdrop-blur-sm overflow-hidden">

                <!-- Decorative Pattern -->
                <div class="absolute top-0 right-0 p-12 opacity-[0.03] pointer-events-none select-none">
                    <i class="fa-solid fa-crown text-9xl"></i>
                </div>

                <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">

                    <!-- Info Column -->
                    <div class="space-y-8">
                        <div>
                            <h3 class="text-2xl font-bold text-white mb-2 flex items-center gap-2">
                                <i class="fa-solid fa-sliders text-red-500"></i> Parâmetros
                            </h3>
                            <p class="text-gray-400 text-sm">Selecione onde o jogo deve rodar. Recomendamos criar um
                                canal dedicado chamado <span
                                    class="text-gray-300 font-mono bg-gray-800 px-1 rounded">#cassino</span>.</p>
                        </div>

                        <!-- Status Display -->
                        <div class="bg-gray-800/40 rounded-2xl p-6 border border-gray-700/50">
                            <h4 class="text-xs font-bold text-gray-500 uppercase tracking-wider mb-4">Estado Atual</h4>

                            <div class="flex items-center justify-between">
                                <div class="flex items-center gap-4">
                                    <div
                                        class="<%= (info.poker && info.poker.state) ? 'bg-green-500/20 text-green-400 border-green-500/30' : 'bg-red-500/20 text-red-400 border-red-500/30' %> h-12 w-12 rounded-xl flex items-center justify-center border">
                                        <i
                                            class="fa-solid <%= (info.poker && info.poker.state) ? 'fa-check' : 'fa-ban' %> text-xl"></i>
                                    </div>
                                    <div>
                                        <div class="text-base font-bold text-gray-200">
                                            <%= (info.poker && info.poker.state) ? 'Módulo Ativo' : 'Módulo Inativo' %>
                                        </div>
                                        <div class="text-xs text-gray-500">
                                            <%= (info.poker && info.poker.state) ? 'Jogadores podem iniciar partidas'
                                                : 'Nenhum jogo permitido' %>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Form Column -->
                    <div class="bg-gray-800/20 rounded-3xl p-8 border border-gray-700/30">
                        <form id="pokerForm" class="space-y-6">

                            <!-- Select Dropdown -->
                            <div class="space-y-2">
                                <label for="channel" class="block text-sm font-bold text-gray-300 ml-1">
                                    Canal de Texto
                                </label>
                                <div class="relative group/select">
                                    <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                                        <i
                                            class="fa-solid fa-hashtag text-gray-500 group-focus-within/select:text-red-500 transition-colors"></i>
                                    </div>
                                    <select id="channel" name="channel"
                                        class="input-dark block w-full rounded-xl py-4 pl-11 pr-10 text-gray-200 placeholder-gray-500 focus:ring-0 sm:text-sm appearance-none cursor-pointer">
                                        <option value="0" <%=(!info.poker || !info.poker.channel) ? 'selected' : '' %>
                                            >-- Selecione o Canal --</option>
                                        <% info4.forEach(function(channel) { %>
                                            <% if (channel.type !==2 && channel.type !==4) { %>
                                                <option value="<%= channel.id %>" <%=(info.poker &&
                                                    info.poker.channel===channel.id) ? 'selected' : '' %>>
                                                    <%= channel.name %>
                                                </option>
                                                <% } %>
                                                    <% }); %>
                                    </select>
                                    <div class="absolute inset-y-0 right-0 flex items-center px-4 pointer-events-none">
                                        <i class="fa-solid fa-chevron-down text-gray-500 text-xs"></i>
                                    </div>
                                </div>
                            </div>

                            <hr class="border-gray-700/50" />

                            <!-- Buttons -->
                            <div class="grid grid-cols-2 gap-4">
                                <button type="button" onclick="submitAction('deactivate')"
                                    class="flex items-center justify-center gap-2 w-full rounded-xl bg-gray-800 text-gray-400 font-bold py-4 hover:bg-gray-700 hover:text-white transition-all border border-transparent hover:border-gray-600">
                                    Desativar
                                </button>

                                <button type="button" onclick="submitAction('activate')"
                                    class="relative group/btn flex items-center justify-center gap-2 w-full rounded-xl bg-gradient-to-r from-red-600 to-orange-600 text-white font-bold py-4 shadow-lg shadow-red-900/20 hover:shadow-red-900/40 hover:scale-[1.02] transition-all overflow-hidden">
                                    <div
                                        class="absolute inset-0 bg-white/20 group-hover/btn:translate-x-full transition-transform duration-500 skew-x-12 -translate-x-[150%]">
                                    </div>
                                    <i class="fa-solid fa-bolt"></i> Salvar Config
                                </button>
                            </div>

                        </form>
                    </div>

                </div>
            </div>
        </div>

    </main>

    <script>
        function submitAction(action) {
            const channelId = document.getElementById('channel').value;
            const guildId = "<%= info4.guildID || info.guildID %>";

            if (action === 'activate') {
                if (channelId === "0") {
                    // Optional: Custom Toast instead of alert
                    alert("Por favor, selecione um canal válido.");
                    return;
                }
                window.location.href = `/bot/pagina/funcs/actpoker/${guildId}/${channelId}`;
            } else {
                window.location.href = `/bot/pagina/funcs/deactpoker/${guildId}/0`;
            }
        }
    </script>
</body>

</html>