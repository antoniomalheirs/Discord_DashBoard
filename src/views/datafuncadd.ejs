<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="../css/cssdir/out.css" rel="stylesheet" />
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <title>Gerenciador de Funções</title>
</head>

<body class="bg-transparent min-h-screen flex items-center justify-center">
  <div
    class="bg-gray-800/80 backdrop-blur-md border border-gray-700 p-8 rounded-2xl shadow-2xl w-full max-w-lg animate-fade-in-up">

    <div class="text-center mb-8">
      <div class="mx-auto flex items-center justify-center h-16 w-16 rounded-full bg-blue-500/20 mb-4">
        <i class="fa-brands fa-youtube text-3xl text-blue-500"></i>
      </div>
      <h3 class="text-2xl font-bold text-white">
        Canal Adicionado!
      </h3>
      <p class="mt-2 text-sm text-gray-400">
        Você receberá notificações de novos vídeos do canal <span class="font-bold text-white">
          <%=nome%>
        </span>.
      </p>
    </div>

    <div class="border-t border-gray-700/50 pt-6">
      <dl class="divide-y divide-gray-700/50">
        <div class="py-4 grid grid-cols-3 gap-4">
          <dt class="text-sm font-medium text-gray-400">Canal</dt>
          <dd class="text-sm font-bold text-white col-span-2 text-right">
            <%=info%>
          </dd>
        </div>
        <div class="py-4 grid grid-cols-3 gap-4">
          <dt class="text-sm font-medium text-gray-400">ID</dt>
          <dd class="text-sm font-mono text-gray-300 col-span-2 text-right">
            <%=info2%>
          </dd>
        </div>
        <div class="py-4 grid grid-cols-3 gap-4">
          <dt class="text-sm font-medium text-gray-400">Servidor</dt>
          <dd class="text-sm font-semibold text-white col-span-2 text-right">
            <%=info1%>
          </dd>
        </div>
      </dl>
    </div>

    <div class="mt-8 flex justify-center">
      <button onclick="voltarAoDashboard()"
        class="bg-blue-600 hover:bg-blue-500 text-white font-bold py-2.5 px-8 rounded-lg transition-all shadow-lg shadow-blue-600/20 transform hover:-translate-y-0.5">
        <i class="fa-solid fa-check mr-2"></i> Concluir
      </button>
    </div>

  </div>
  <script>
    function voltarAoDashboard() {
      window.history.back();
    }
  </script>
</body>

</html>